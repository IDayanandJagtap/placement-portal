import { Stack, Text } from "@chakra-ui/react";
import React from "react";

const Settings = () => {
    return (
        <Stack px={[4, 6]} py={4} mb={[8, 8, 8, 4]} minH={"90vh"}>
            {/* delete account and logout option */}
            <Text>Delete account and logout option</Text>
        </Stack>
    );
};

export default Settings;
